# FF14 スキル回しシミュレーター 開発計画

## 目的

FF14のスキル回しを時間軸に沿って試せる静的Webアプリを作る。
ユーザーはスキルボタンを押しながら、威力合計、コンボ、GCD、リキャスト、硬直、バフ/デバフ、DoT、ジョブ専用ゲージなどを確認できる。
最初に対応するジョブはリーパーとする。

## 現在の開発前提

- 優先度AのMVP整備と、優先度Bの時間軸・GCD・硬直の最小実装まで進んでいる。
- アプリはHTML/CSS/JavaScriptだけで動く静的Webアプリとして継続する。
- 最終的な方向性は時間軸ベースのローテーションシミュレーター。
- 初期UIはスキルボタン押下式でよいが、内部状態は経過時間を中心に管理する。
- 威力は各スキルに設定された威力を加算する。ステータス、乱数、クリティカル、ダイレクトヒット、防御力などを含む実ダメージ計算はまだ扱わない。
- FF14の正確なスキル値を入れる場合は、対象パッチを確認してから反映する。
- リーパーのアクション値は公式ジョブガイドのPatch 7.5・Lv100表示を基準にする。

## 開発方針

- 小さな動作状態を保ちながら機能を追加する。
- スキルデータ、状態管理、計算ロジック、画面表示を分ける。
- リーパーで必要な仕組みを先に作り、複数ジョブ対応は後回しにする。
- 厳密なFF14再現を最初から狙わず、段階的に近づける。
- UIはシミュレーター本体を最初に表示し、ランディングページ化しない。
- 現行デザインに反映済みの画面状態を、UI実装時の最新仕様として扱う。
- 以前の仕様メモと現行デザインが食い違う場合は、原則として現行デザインを優先する。

## 現行デザイン仕様

基準デザイン:

- Figma file: FF14 Simulator Wireframes
- Figma URL: https://www.figma.com/design/iwCXpuYFsOcUCJ7rfxYYXQ/FF14-Simulator-Wireframes?node-id=73-284
- Page: デザイン案
- Frame: デザイン案 v6 説明削除 GA4管理画面風
- Node: `73:284`

全体方針:

- GA4管理画面に近い、白ベースで実用的なツールUIにする。
- 背景は薄いグレー、主要パネルは白、境界は1pxの明確なグレー線で整理する。
- セクションの始まりは淡いヘッダー帯や区切り線で分かるようにする。
- 画面上の説明文は置かず、ラベル、数値、アイコン、状態差で意味が伝わるUIにする。
- 装飾よりも視認性、情報密度、反復操作のしやすさを優先する。
- 全体を囲う大きなapp shellは使わず、背景上に各パネルを直接配置する。

表示構成:

- タイムラインを上段の主役として扱う。
- 経過時間と合計威力はタイムライン右上に配置する。
- ヘッダー右上はリセットのみとし、破壊的操作を通常入力から少し離す。
- 待機ボタンは、次の入力として時間を進める操作としてスキル操作エリア側に置く。
- 1手戻すは、配置済みタイムラインを編集する操作としてタイムライン内に置く。
- ジョブゲージ、スキル操作、状態表示は白いパネルと細い枠線で整理する。

タイムライン表示:

- タイムライン上は、配置済みアクション、表示位置インジケーター、威力判断に関わる効果帯を中心にする。
- 次の配置位置マーカーは常時表示しない。
- 硬直線は常時表示しない。硬直は内部計算と必要時の警告に寄せる。
- 効果帯は、デスデザイン、アルケインサークル、薬のような威力上昇判断に関わるものへ絞る。
- タイムラインはGCD、アビリティ、バフ帯の行が読みやすいように行区切りを持たせる。
- 1画面の表示尺は35秒相当で固定し、35秒を超えるアクションが追加されたときだけ同じ縮尺のまま右方向へ延長する。
- バフ／デバフの終了時刻や待機操作だけでは表示尺を延長せず、効果帯は現在のタイムライン末端で切る。
- 延長後にアクションを追加した場合は、横スクロールを最新位置へ自動追従させる。
- 威力系の効果帯はデバフを上段、バフを下段に分けて重なりを避ける。

状態表示:

- 状態欄には実データとして扱うものだけを表示する。
- 状態欄は上段をバフ、下段をデバフ／DoTとして分離し、シミュレーター内部状態は表示しない。
- 予約枠や仮のDoT/デバフ項目は置かない。
- 固有ゲージではなく時間制限つき状態として扱えるものは、バフ/状態側に寄せる。

スキル表示:

- スキルボタンはアイコン風の表示を前提にする。
- 使用不可状態は暗く表示する。
- コンボ推奨や選択中などの注目状態は、反転や強調で分かるようにする。
- 薬などの重要な強調にはアクセント色を使う。

現時点で常時表示しないもの:

- PPS/DPS相当値。
- 操作履歴ログ。
- 現在のコンボ段階の数値。
- GCD残り時間の大きな単独表示。
- バフ/デバフの効果量。
- 対象が自分か敵かを説明するテキスト。
- リアルタイム進行前提の一時停止/再開UI。

## 優先度A: MVP整備

- 日本語表示とコメントの文字化け修正。
- MVP向けUIラベルの整理。
- リセットボタンの追加。
- スキル履歴ログの見やすさ改善。
- スキルデータ形状の整理。
- 基本的なコンボ判定と威力合計の維持。

現状メモ:

- 計画書はUTF-8の日本語に戻した。
- アプリ本体のHTML/JavaScriptにはまだ文字化けが残っているため、次のUI修正時に直す。

## 優先度B: 時間軸シミュレーター化

実装済み（2026-07-31）:

- 経過時間、GCD再使用可能時刻、アクション硬直終了時刻を状態に追加。
- 使用時刻つきタイムライン、GCD/硬直残り時間、使用不可表示を追加。
- 「次まで待機」で次の再使用可能時刻へ進める最小操作を追加。
- スキルデータに `gcdRecast` と `animationLock` を追加。

実装済み（2026-07-31）:

- 個別リキャスト、チャージ、アビリティ挟みを実アクションへ適用。
- GCD食い込みの記録とタイムライン強調。
- 確認用タイムインジケーター、過去時刻の威力・ゲージ・状態・使用可否再現。

1. 経過時間を状態として追加する。
2. タイムライン上にアクションの使用時刻を表示する。
3. GCDスキル使用時にGCD状態を進める。
4. アビリティとウェポンスキルを明確に区別する。
5. スキルごとのリキャスト状態を管理する。
6. 使用できないスキルの理由を画面で分かるようにする。
7. 簡易タイムラインを表示する。
8. タイムインジケーターで過去時刻の状態を確認できるようにする。
9. PPS/DPS相当値は常時表示せず、将来的なグラフ化や区間分析の候補として扱う。
10. 現行デザインに合わせ、操作履歴ログではなくタイムラインを主要な確認UIにする。

## タイムインジケーター仕様

タイムインジケーターは、編集位置ではなく確認用の表示カーソルとして扱う。

- インジケーターをタイムライン上で動かすと、その時刻時点の状態を画面に表示する。
- 対象状態は、合計威力、ジョブゲージ、バフ/デバフ/DoT、リキャスト、コンボ推奨表示、スキル使用可否など。
- インジケーターが過去を指している間は、過去時刻を確認中であることが分かる表示にする。
- 過去確認中は「最新へ戻る」操作を用意する。
- スキルボタンを押した場合は、インジケーター位置に関係なく常にタイムライン末尾へ追加する。
- 過去確認中にスキルボタンを押した場合は、自動的に最新位置へ戻ってから末尾へ追加する。
- 通常のスキルボタン押下では、過去時刻への挿入や置き換えは行わない。
- 現在時刻、表示時刻、タイムライン末尾時刻は内部的に区別できる設計にする。

## タイムライン編集仕様メモ

タイムライン途中の修正は、通常のスキルボタン押下とは別の編集操作として扱う。

想定する編集操作:

1. タイムライン上のアクション削除。
2. タイムライン上のアクション置き換え。
3. スキルアイコンのドラッグアンドドロップによるアクション間への挿入。
4. タイムライン上のアクション移動。

初期実装:

- 最初はタイムライン上のアクション削除のみ対応する。
- 削除後は、その位置以降の時刻と状態を再計算する。
- 置き換え、挿入、移動は初期実装には含めない。

編集後の基本ルール:

- 編集した位置以降の時刻と状態を再計算する。
- ゲージ不足、リキャスト未完了、コンボ不成立など、編集によって矛盾が生じた場合は画面上で分かるようにする。
- 初期実装では自由な時間指定配置は避け、既存アクションの前後に挿入する形を優先する。
- ドラッグアンドドロップによる挿入や移動は便利だが、判定、再計算、保存、共有、競合管理が複雑になるため後回しにする。
- ドラッグ挿入や移動を本格実装する場合は、静的フロントエンドだけで扱うか、保存・共有・検証用のバックエンドを導入するかも含めて検討する。
- 編集操作の詳細UIは未決。ワイヤー検討では、まず削除操作の見せ方、確認ダイアログの要否、削除後の再計算結果の見せ方を決める。

## 硬直モデルの整理

参考文献:

- Hosiume San「〖5.x〗アビリティ硬直についての検討、および黒魔のSS特化の弱点についての確認」
  https://jp.finalfantasyxiv.com/lodestone/character/26041085/blog/4467436/
- 関連日記や引用では、WS/アビリティの発動硬直はおおむね0.66〜0.67秒程度として扱われている。

MVPで採用する簡易モデル:

- デフォルト硬直は `0.67` 秒とする。
- `animationLock` はスキル使用直後から次のアクション入力が通るまでの最短待ち時間として扱う。
- GCDスキルもアビリティも、まずは同じ `0.67` 秒硬直を持つものとして扱う。
- 将来の全ジョブ対応では、竜騎士のスターダイバーのように硬直が長いアクションを個別例外として扱う。
- 個別硬直が必要なスキルは `animationLock` をスキルデータ側で上書きする。未指定のスキルは既定値 `0.67` 秒を使う。
- エンジン側は `skill.animationLock ?? DEFAULT_ANIMATION_LOCK` のように、最初から個別上書きを受け取れる形にしておく。
- スターダイバーのような例外値は、対象パッチと検証値を確認してから登録する。仮置きする場合は暫定値として明記する。
- GCDスキルは `gcdRecast` を開始し、次のGCDスキルは `gcdReadyAt` まで使えない。
- アビリティはGCDを開始しないが、硬直中は次のアクションを使えない。
- 1つのGCD間にアビリティを何個挟めるかは、`gcdReadyAt` と `actionReadyAt` の比較で判定する。
- 2アビ挟みでGCDに食い込むかどうかは、`actionReadyAt > gcdReadyAt` なら食い込みとして記録する。

最初は扱わないもの:

- プレイヤーの入力遅延。
- 通信遅延やFPSによる差。
- 先行入力受付の細かい仕様。
- スキルごとの固有硬直差。
- 詠唱完了後硬直やキャスター固有の追加硬直。

将来拡張:

- 設定画面で `baseAnimationLock` を変更できるようにする。
- スキルごとに `animationLock` を上書きできるようにする。
- 長硬直アクションは `animationLock: 1.5` のようにスキルデータへ持たせる。ただし数値は検証・パッチ確認後に確定する。
- `inputDelay` や `latencyBuffer` を追加し、理想入力と現実寄り入力を切り替えられるようにする。
- 食い込み時間を履歴とタイムラインに表示する。

## 優先度C: リーパー対応（単体回し完了）

Patch 7.5実装（2026-07-31）:

- 単体基本コンボ3段、ソウルゲージ増加を実装。
- シャドウ・オブ・デス、デスデザイン30秒／最大60秒延長、10%補正を実装。
- ソウルスライスの2チャージ管理と、ストークスウェーズのソウルゲージ50消費を実装。
- ジビトゥ／ギャロウズ、グラトニー、エクスジビトゥ／エクスギャロウズを実装。
- ソウルゲージ、シュラウドゲージ、妖異の鎌、処刑人を実装。
- レムールシュラウド、レムール／ヴォイドスタック、ヴォイド／クロスリーパーを実装。
- レムールスライス、サクリフィキウム、コムニオを実装。
- アルケインサークルの20秒・3%補正とタイムライン効果帯を実装。
- 単体向け主要回しを通してバースト区間を検証できる状態にした。

1. リーパーを初期ジョブとして固定または選択状態にする。（完了）
2. リーパーの主要ウェポンスキルを登録する。（完了）
3. リーパーの主要アビリティを登録する。（完了）
4. ソウルゲージを追加する。（完了）
5. シュラウドゲージを追加する。（完了）
6. レムールシュラウド中の状態を管理する。（完了）
7. レムール/ヴォイドのスタックを管理する。（完了）
8. バースト中のスキル回しを検証しやすくする。（完了）

## 優先度D: バフ/デバフ/DoT

実装済み（2026-07-31）:

- バフ／デバフの付与時刻、終了時刻、残り時間表示。
- デスデザイン10%とアルケインサークル3%の威力補正。
- 同一効果の再付与・延長時に連続したタイムライン効果帯を維持。
- ロールアクションを含む実装済み効果を、状態欄のバフ／デバフへ分離表示。
- 現在のリーパーアクションセットには継続威力DoTがないため、DoTティック計算の実データ適用は保留。

完了範囲:

- バフ／デバフ区間内に入ったアクション数と威力を効果帯へ集計。
- 将来DoTを持つジョブを追加する際の3秒ティック処理は、対象データ追加時に行う。

1. バフ効果時間を管理する。
2. バフ中の威力補正を反映する。
3. デバフ効果時間を管理する。
4. DoTの継続ダメージを時間軸に反映する。
5. バフ/デバフの残り時間を画面に表示する。
6. バフ内に入った威力を集計できるようにする。

## リーパー全アクション対応

- Patch 7.5・Lv100の専用アクション38件を登録済み。
- 近接物理DPSロールアクション6件を登録済み。
- 通常／範囲コンボ、移動、防御、共有リキャスト、チャージ、派生アクションを含む。
- ソウルソウ→ハーベストムーン、アルケインサークル→プレンティフルハーベスト、コムニオ→ペルフェクティオを実装。
- アルケインクレストのバリア破壊後回復と、アームズレングスの被弾時スロウは、被ダメージイベント未実装のため自動発動対象外。
- 公式ジョブガイドが示すアクション置き換わり（同一ホットバー枠内での表示・実行内容の入れ替わり）をUIへ反映（2026-08-01）。
  - ジビトゥ／ギャロウズ／ギロティン ⇄ ヴォイドリーパー／クロスリーパー／グリムリーパー（レムール中） ⇄ エクスジビトゥ／エクスギャロウズ／エクスギロティン（処刑人所持中）。
  - ストークスウェーズ⇄レムールスライス、シーフスウェーズ⇄レムールサイズ（レムール中）。
  - グラトニー⇄サクリフィキウム（レムール中）、コムニオ⇄ペルフェクティオ（実行可時）、ソウルソウ⇄ハーベストムーン（状態中）。
  - ヘルズイングレス／イーグレス⇄リグレスは移動系で威力に影響せず、リグレス自体が未実装のため対象外（将来検討）。

## 優先度E: 操作性と検証支援

1. タイムラインから1手戻せるようにする。
2. タイムライン途中のアクション削除をできるようにする。
3. スキル回しを保存・読み込みできるようにする。
4. 結果をコピーできるようにする。
5. スキルアイコン風のボタン表示にする。
6. モバイルでも操作しやすいレイアウトにする。
7. 将来的に、置き換え、ドラッグ挿入、移動を検討する。

実装済み（2026-07-31）:

- 1手戻す、タイムライン上のアクション削除と後続再計算。
- ブラウザ内への回し保存・読込。
- アイコン風ボタン、デスクトップ／モバイル向けレイアウト。
- 置き換え、ドラッグ挿入、移動は将来候補のため現行完了条件には含めない。

## スキルデータ設計メモ

スキルには段階的に次の情報を持たせる。

- `id`: スキルID
- `job`: 対象ジョブ
- `name`: スキル名
- `type`: `weaponskill` / `ability` / `spell` など
- `potency`: 通常威力
- `comboPotency`: コンボ成功時の威力
- `requiredComboStep`: 必要なコンボ段階
- `comboStep`: 使用後のコンボ段階
- `recast`: 個別リキャスト時間
- `gcd`: GCD対象かどうか
- `gcdRecast`: GCDリキャスト時間
- `animationLock`: 硬直時間
- `gaugeCost`: 消費するジョブゲージ
- `gaugeGain`: 増加するジョブゲージ
- `effects`: バフ、デバフ、DoT、スタック増減などの効果

## 状態管理メモ

時間軸化後の状態には次の情報を持たせる。

- 経過時間
- 表示中の時刻
- タイムライン末尾の時刻
- 合計威力
- 現在のコンボ段階
- GCDが次に使用可能になる時刻
- 次のアクションが使用可能になる時刻
- スキルごとのリキャスト状態
- 使用履歴
- 発動中のバフ
- 付与中のデバフ/DoT
- リーパーのソウルゲージ
- リーパーのシュラウドゲージ
- レムール/ヴォイド関連スタック

## 優先度F: 今後検討する拡張候補（未着手・2026-08-01メモ）

- 戦闘開始前のカウント時間（マイナス秒側の助走区間）を導入する。
- カウント中でも、詠唱のように事前入力できるアクション（ハルパーなど）は実行可能にし、戦闘開始0秒で着弾するような扱いができるようにする。
- フェーズの要素（区切り／切り替わり）をタイムラインに追加する。
- 待機時間のタイムライン表示をもう少し分かりやすくする。

いずれもユーザー要望として記録した段階で、要件・UI設計はまだ詰めていない。着手時は個別に要件を確認する。

## 次にやること

リーパー単体シミュレーターの計画済み必須機能は完了。以降は、他ジョブ追加、被ダメージイベント、自由な置き換え／ドラッグ編集、優先度Fの拡張候補などを要件確定後に進める。

## React + TypeScript + Vite 移行 (2026-08-03開始、進行中)

計画書: `C:\Users\toshiki\.claude\plans\declarative-gliding-falcon.md`(既存Claude Codeセッションのplanファイル。
移行の背景・決定事項・型設計・エンジン層設計・マイルストーン定義がすべてここにある。次回セッションは
まずこれを読むこと)。

**方針(承認済み)**: 既存のバニラJS実装を`legacy/`へ完全退避し、リポジトリルートに新規React+TS+Viteアプリを
構築。移行スコープはリーパー単体回しの機能パリティのみ(他ジョブ対応は保留)。型設計だけは多ジョブ対応を
見据えて汎用化済み。ドラッグ&ドロップはdnd-kit等のライブラリを使わず、既存のPointer Eventベース実装を
カスタムReact hooksへ移植する方針。テストはVitest(+ `@testing-library/react`)を新規導入。

### 完了したマイルストーン(M0〜M6) — すべて検証済み

- **M0**: `legacy/`へ`git mv`で退避(index.html/css/js)。`assets/`は`legacy/assets/`へコピー(ルートの`assets/`
  はM1で新アプリへ移動するため残していた)。
- **M1**: ルートに`npm create vite@latest . -- --template react-ts`相当でscaffold。`package.json`の
  `name`を`ff14-simulator`に変更。アイコンを`assets/icons/`→`public/assets/icons/`へ移動(45個)。
- **M2**: `src/types/{skill,state,history,job}.ts`、`src/data/reaper/{skills,actionSlots,buffNames,types,
  jobDefinition}.ts`。旧`skills.js`の45スキル(手書き18+`createAction`ファクトリ27)を1件ずつ忠実に転記。
  `requirements`を`{stacks, flags, buff, buffAbsent}`の汎用形へ、`gauges`/`jobState`を
  `Record<string, number>`/`Record<string, JobStateEntry>`(counter/flag/mode の3形状)へ一般化。
- **M3**: `src/engine/{time,potency,cooldowns,gating,effects,editOps,replay,jobs/reaper/*}.ts`。
  旧`engine.js`(865行)の全関数を純粋関数として移植。**重要な発見**: 旧アプリは「ライブクリック」(個別
  リキャストが空いていないと拒否)と「undo/削除/移動などの再構築」(個別リキャストの空き時刻まで自動で
  待機time-warpする、より寛容な挙動)で扱いが異なっていた。これを`getLiveAppendRejectionReason`
  (`engine/gating.ts`)という専用関数で再現し、Node上で旧`js/engine.js`を実際に動かした数値
  (`scripts/cross-check-legacy.cjs`で再現可能)と突き合わせて一致を確認済み。
- **M4**: `src/components/{Header,TimelinePanel,GaugePanel,SkillPanel,SkillButton,StatusPanel}.tsx` +
  `src/styles/global.css`(旧CSSをほぼそのまま移植)。固定サンプル回しで静的表示を確認(ユーザーが
  ブラウザで目視確認済み、「見た目は問題ない」との回答あり)。
- **M5**: `src/hooks/useSimulator.ts`。`entries`(React state)→`replay()`→派生状態、というアーキテクチャで
  クリック操作を実装。`@testing-library/react`の`renderHook`で実際のフック経由の統合テストを追加し、
  legacy版のライブクリック数値と一致することを確認。
- **M6**: `src/hooks/useTimelineScrub.ts` + `src/hooks/useTooltipController.ts` +
  `src/components/SkillTooltip.tsx`。タイムラインのプレイヘッドドラッグでの過去確認、スキルボタンの
  ホバーツールチップを実装。スクラブしても`final`(最新状態)が変化しないことをテストで確認済み。

Vitest: 27件全てグリーン。`tsc --noEmit`、`npm run build`ともにクリーン。

### M7 ドラッグ&ドロップ移植 — 完了(2026-08-03)、タイル移動ドラッグは2026-08-28に削除

> **2026-08-28追記**: 以下の「配置済みタイルの移動ドラッグ」機能(`useTimelineTileDrag.ts`、
> `moveEntry`/`buildEntriesAfterMove`)はユーザーからの指摘により削除した。理由・詳細は
> 「UI不具合5件の修正 — 完了(2026-08-28)」セクションの項目1を参照。スキルパネルから
> タイムラインへの新規挿入ドラッグ(`useSkillInsertDrag.ts`)は引き続き有効。

- `src/hooks/useTimelineTileDrag.ts`を新規作成。タイムライン上の配置済みタイル移動+ホバー×削除ボタン。
  ドラッグ中は`item.style.left`をrefで直接DOM操作し(Reactの再レンダーを経由しない)、`pointerup`時のみ
  `dispatch.moveEntry`を呼ぶ2段階方式。pointercancel時はドラッグ開始時に記録しておいた元の`style.left`
  文字列を直接書き戻す方式を採用(強制再レンダーに頼る案は、React が同一値の style props を再適用しない
  ため機能しないことが判明したための実装判断)。
- `src/hooks/useTimelinePan.ts`を新規作成。`.timeline-scroll`へネイティブ`addEventListener`で直接配線
  (子要素のドラッグ操作と競合しないよう、legacyと同じくmarker classでの除外判定を踏襲)。
- `App.tsx`に`chartRef`/`gcdTrackRef`/`abilityTrackRef`/`scrollRef`を追加し、`TimelinePanel`/`SkillPanel`
  両方へpropsとして配線。`useDragGhost`もApp.tsxでインスタンス化。
- `TimelinePanel.tsx`: 外部refを実DOM要素へ割り当て、`useTimelinePan`を`.timeline-scroll`に配線。
  `TimelineTile`へ`useTimelineTileDrag`を配線し、ホバー×削除ボタンを追加。
- `SkillPanel.tsx`→`SkillButton.tsx`: `useSkillInsertDrag`を配線。

**動作確認済み(ブラウザ、PointerEvent直接dispatchによる決定論的テスト)**:
- タイル移動(異なるスキルを含む回しで、移動後にコンボ再計算・威力再集計まで正しく反映されることを確認)。
- ホバー×削除→以降の時刻再計算。
- pointercancel時の位置復元(`style.left`が移動前の値に戻り、`is-dragging`クラスも外れる)。
- スキルボタン→タイムラインへのドラッグ挿入(ゲージ不足で却下されるケース・成功して配置されるケースの両方)。
- `tsc --noEmit`・`npm run build`・Vitest 27件、すべてクリーン。

**デバッグ時の学び**: CDPベースのブラウザ自動化ツール(`computer`アクションの`left_click_drag`)は、
このセッション内で座標スケール(視覚的スクリーンショット座標→実CSS座標の倍率)が測定タイミングにより
`1.34倍`だったり`1:1`だったりと不安定で、かつpointerdown/upイベント自体を落とすことがあった
(pointermoveだけ発火してdown/upが欠落するケースを複数回観測)。以後この種の検証をする際は、
`javascript_tool`で`el.dispatchEvent(new PointerEvent(...))`を直接使う方が確実。

### M8: localStorageへの自動永続化 — 完了(2026-08-14)

legacy版の「保存/読込ボタン」は採用せず、要件ヒアリングの結果「サイトを離れても再訪時に入力状態が
残っていること」のみが必須と判明したため、entries/settingsの変更を都度自動でlocalStorageへ書き込み、
マウント時に自動復元する方式にスコープを変更した。

- `src/hooks/persistence.ts`を新規作成。`loadPersistedState(job)`/`savePersistedState(jobId, entries, settings)`。
  保存キーは`ff14-simulator:${job.id}`。現在のジョブに存在しないskillIdを含むエントリや壊れたJSONは
  黙って除外し、例外を投げない(legacy版の読込バリデーションと同じ方針)。
- `useSimulator.ts`の`entries`/`settings`の初期値を`loadPersistedState`から取得するよう変更し、
  `useEffect`で変更の都度`savePersistedState`を呼ぶよう配線。手動save/loadボタン・`dispatch.save`/
  `dispatch.load`は追加していない(不要と判断)。
- `useSimulator.test.ts`に`beforeEach(() => localStorage.clear())`を追加(自動永続化により前のテストの
  状態が次のテストへ漏れるのを防ぐため)。M8用のテスト5件を追加(再訪時の復元、未保存時は空、存在しない
  スキルを含むエントリの除外、壊れたJSONの無視、reset()後は空で復元されること)。
- `tsc --noEmit`・`npm run build`・Vitest 32件(既存27+新規5)、すべてクリーン。
- 詳細な意思決定の経緯は`.company/secretary/notes/2026-08-14-decisions.md`参照。

### M9: CSV書き出し/読込 — 完了(2026-08-27)

要件ヒアリングの結果、.xlsx対応は不要でCSVのみで進める方針に確定。

- `src/hooks/csv.ts`を新規作成。`historyToCsv(history)`/`csvToEntries(csvText, job)`。
  追加ライブラリなしの自前CSVパーサ/ライタ(カンマ・引用符・改行を含むフィールドのクォート処理込み)。
- CSV列は`order,kind,skillId,skillName,duration,usedAt,potency`。再読込に使うのは`kind`/`skillId`/
  `duration`のみで、`usedAt`/`potency`は確認用の付加情報(再読込時は無視)。再構築したエントリは常に
  `usedAt:0, preserveTiming:false`とし、行の並び順どおり自然な再生タイミング(通常のスキルボタン押下と
  同じ規約)でreplay()し直す。
- 現在のジョブに存在しないskillId、不正な`duration`の行は黙って除外し、除外件数を`skippedRows`として
  返す(persistence.tsのlocalStorage読込と同じ「黙って除外」方針)。
- `src/engine/editOps.ts`の`generateEntryId`をexportして再利用。
- `useSimulator.ts`に`loadEntries(newEntries, skippedRows)`を追加。CSV読込時、除外件数>0なら
  「CSV読込: N件の行を読み込めず除外しました」を`message`に表示。
- `Header.tsx`の既存の未配線ボタン(旧「保存」「読込」、M8で自動永続化に切り替えた際に放置されていた)を
  「CSV書き出し」「CSV読込」に転用。`App.tsx`で書き出しはBlob+`<a download>`、読込は隠し
  `<input type=file>`を使う実装(ライブラリ追加なし)。
- テスト: `src/hooks/__tests__/csv.test.ts`(往復変換、不正skillId/duration行の除外、ヘッダー不正、
  空入力の5件)を新規追加。`tsc --noEmit`・`npm run build`・Vitest 37件(既存32+新規5)、すべてクリーン。
- ブラウザでの実動作確認済み(Chrome拡張経由。CSV書き出しのBlob内容をJSで検証、CSV読込を
  `DataTransfer`経由のファイル選択で再現し、無効行の除外メッセージ表示まで確認)。

### M10: 画像出力 — 完了(2026-08-27)

要件ヒアリングの結果、対象範囲はタイムライン全体(GCD/アビ/効果時間の行を含む現在の表示)に確定。
実装方針は「軽量ライブラリを使う」を選択(SVG foreignObject等の自前実装は見た目の再現性リスクが
高いと判断)。

- `html-to-image`パッケージを追加(`npm audit fix`でtransitiveなnanoid脆弱性も解消済み)。
- `src/hooks/exportImage.ts`を新規作成。`exportTimelineImage(node, fileNamePrefix)`が
  `toPng()`でPNGを生成し、`<a download>`経由でダウンロードする。キャプチャ対象は`timeline-chart`
  要素そのもの(タイムスクロール領域の祖先ではなく、実際の全幅を持つ要素を直接指定することで、
  横スクロールで隠れている範囲も含めた全体を欠けなく画像化)。`pixelRatio: 2`で高解像度化。
- `Header.tsx`/`App.tsx`に「画像出力」ボタンを追加(CSV書き出し・CSV読込の隣)。

**副次的に発見・修正した既存バグ**: `vite.config.ts`の`base: '/FF14-Simulator/'`
(GitHub Pages配信用)に対し、`TimelinePanel.tsx`/`SkillButton.tsx`/`DragGhost.tsx`のスキル
アイコン`<img src>`が`/assets/icons/...`という絶対パス直書きになっており、base配下では常に404
していた。各コンポーネントの`onError`ハンドラがサイレントにフォールバック表示へ切り替える設計だった
ため、開発中も気づかれていなかった(スキルボタン・タイムライン・ドラッグゴーストのアイコンが実際には
一度も表示されていなかった)。`import.meta.env.BASE_URL`を先頭に付与する形に3箇所修正し、
アイコンが正しく表示されることをブラウザで確認した。画像出力の実装中、`html-to-image`が404画像で
例外を投げたことで発覚した。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン(画像出力自体はDOM/Canvas依存の
  ブラウザ統合機能のためユニットテスト対象外。M7のドラッグ&ドロップと同じ方針)。
- ブラウザでの実動作確認済み(Chrome拡張経由。生成PNGを`Image`でデコードして2508×552pxである
  ことを確認、ページに挿入して視覚的にも8アクション+デスデザイン効果帯+インジケーターが正しく
  描画されていることを確認)。

### UI不具合5件の修正 — 完了(2026-08-28)

実際に使ってみたユーザーからの指摘5件に対応。

1. **配置済みタイルのドラッグ移動を削除**: `useTimelineTileDrag.ts`を削除し、`TimelineTile`から配線を
   除去。`useSimulator.ts`の`moveEntry`、`editOps.ts`の`buildEntriesAfterMove`、`TimelinePanel`の
   `onMoveEntry`propも連鎖的に削除(`Insertion`型もskill専用に簡略化)。CSSの`.is-dragging`関連ルールも
   削除。スキルパネル→タイムラインへの新規挿入ドラッグ(`useSkillInsertDrag.ts`)は対象外、維持。
2. **効果帯の名前の隣に出ていた威力数値(例: 9552)を削除**: `TimelinePanel.tsx`の効果帯ラベルから
   `{effect.potency ? \` ${effect.potency}\` : ""}`を削除し、名前のみ表示に変更(数値は`title`属性の
   ホバーツールチップに残置)。PLAN.mdの現行デザイン仕様に「バフ/デバフの効果量は常時表示しない」と
   明記されていたにもかかわらず実装が数値を出していた、という既存の仕様逸脱だった。
3. **アクション追加時のタイムライン自動スクロール**: 従来はスクロール位置を一切制御しておらず、
   ライブ入力でタイムラインが伸びても表示範囲が追従しなかった(常に先頭に留まる一方、表示位置
   インジケーターだけが画面外へ進んでしまう)。要件確認の結果「最新のアクションに自動追従」を採用。
   `TimelinePanel.tsx`に`prevHistoryLengthRef`を追加し、`history.length`が増えた(=ライブ追加された)
   かつプレビュー中でない場合のみ`scrollLeft`を末尾へ移動するuseEffectを実装。プレビュー(スクラブ)中は
   追従しない。
4. **待機ブロックがアクションアイコンの上に重なって隠す問題**: 待機は直前アクションと同じ時刻
   (`elapsedTime`)から始まることが多く、アクションアイコン(44px固定幅)が後続の待機ブロックの位置まで
   視覚的に覆ってしまうことがあった。`.timeline-action`にz-index:1、`.timeline-action.is-wait`に
   z-index:0を明示し、アクションが常に待機より前面に出るようにした。
5. **待機ブロックにホバーしても削除×が出ない問題**: 2段階の原因があった。(a) `.timeline-action.is-wait`
   の`overflow:hidden`が、削除×(`top:-7px;right:-7px`という箱の外側の位置)をクリップしていた。
   (b) 待機は直前アクションと同じ時刻から始まることが多く(上記4参照)、アクション(z-index:1)が待機
   (z-index:0)を覆っている場合、CSSのスタッキングコンテキストの入れ子の性質上、待機の中の子要素に
   どれだけ高いz-indexを与えても親(待機本体)のスタッキング階層を越えて前面には出せない
   ―― そのため待機自体へマウスの当たり判定すら届かず、ホバーそのものが発火しないケースがあった。
   これを解消するため、待機の削除×は待機本体の**子要素ではなく独立した兄弟要素**として
   `TimelinePanel.tsx`内で描画し直し、`.timeline-wait-delete`にz-index:2(アクションより前面)を
   与えたうえで、ホバー判定に頼らず**常時表示**にした(待機はアクションほど密に並ばないため、常時表示
   による視認性低下は許容範囲と判断)。アクション側の削除×は従来通りホバー時のみ表示。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン(既存テストの修正は不要、削除した
  `moveEntry`等はテスト対象外だったため)。
- ブラウザでの実動作確認済み(Chrome拡張経由)。特に項目5は、`getBoundingClientRect`/
  `elementFromPoint`/`matches(':hover')`を使った実座標でのヒットテスト検証まで行い、削除×ボタンが
  実際に最前面でクリック可能であることと、クリックでエントリが削除されることを確認した。

### タイムラインUI追加修正4件 — 完了(2026-08-28)

前回のUI不具合5件修正を実際に使ってみたユーザーからの追加指摘4件に対応。

1. **カウント設定時、最初のアクション配置で35s地点までスクロールしてしまう**: 自動追従の実装が
   `scrollLeft = scrollWidth - clientWidth`(常にスクロール可能な最大値へ飛ぶ)になっていたのが原因。
   ベースの表示尺(35秒分)は常に確保されているため、アクションが1つしかなくても「スクロール可能な
   最大値」はベース末尾(35s)を指してしまい、実際の最新アクション位置とは無関係だった。表示位置
   インジケーターの実座標(`latestPositionPx`)を基準に、その位置が見える最小限のスクロール量
   (`latestPositionPx - clientWidth + margin`)へ変更。
2. **効果帯(デスデザイン等)の名前が横スクロールで見えなくなる**: 名前ラベルを`.timeline-effect-label`
   として`position: sticky; left: 4px;`に変更し、帯自体が表示されている間は名前が追従して見える
   ようにした。当初、親の`.timeline-effect`に残っていた`overflow: hidden`がstickyの基準スクロール
   コンテナ判定を狂わせ(親自身を基準にしてしまい実質何もしない状態になっていた)、全く追従しない
   問題が発生したため、`overflow: hidden`を削除して対応(帯の範囲を超えて名前が飛び出さない制約は
   sticky自身の性質でまかなえるため実害なし)。
3. **待機の削除×、常時表示にしたが反応せずクリックできない**: そもそも「ホバー時のみ表示」の挙動を
   崩したくないというユーザー要望もあり、常時表示ではなくホバー限定表示へ戻した。ただし待機は
   直前アクションと同じ時刻から始まることが多く、アクション(z-index:1)に覆われている間はCSSの
   `:hover`だと待機自体へマウスの当たり判定が届かずホバーが発火しない問題が残る。React state
   (`WaitTile`コンポーネントを新設し`hovered`を管理)に切り替え、待機の高さをGCD行+アビリティ行
   ちょうど2行分(117px)まで伸ばして、アクションと重ならない領域を安定して確保することで、
   その領域でのホバーが確実に成立するようにした。削除×は`hovered`が真のときだけ描画する独立した
   兄弟要素(`.timeline-wait-delete`)のまま。ボタン本体と削除×を`display: contents`の共通親でくくり、
   削除×自体にマウスが乗ってもグループとしてホバー継続扱いにして、出た瞬間に消えるちらつきを防止。
4. **待機がアクションに重なると短い秒数ほど見えにくい**: ユーザー提供の参考画像に合わせ、
   `.timeline-action.is-wait`の`border-radius`を`0`にして角丸をなくし、タイムラインを縦断する
   ような見た目にした(点線枠・斜線塗り・文字はそのまま維持)。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン。
- ブラウザでの実動作確認済み(Chrome拡張経由)。ネイティブの`mouseover`/`mouseout`イベントを
  `dispatchEvent`で直接発火させ、ホバーで削除×が出現→クリックで削除→グループ外へ出ると非表示に
  戻る、の一連の流れと、削除×自体にカーソルが乗ってもちらつかないことを確認した(CDP経由の
  クリック/ホバー座標変換は本セッション中に不安定な挙動が再現したため、この確認方法を採用)。

### タイムラインUI追加修正4件・第2弾 — 完了(2026-08-28)

上記4件を実際に使ってみたユーザーからの、さらなる追加指摘4件に対応。

1. **自動スクロールがアイコンの左端基準で、右側が見切れる**: `latestPositionPx`は着弾時刻
   (アイコンの左端)の座標であり、アイコン自体はそこから右へ最大44px(GCDアイコン幅)描画される。
   スクロール先の計算に`iconWidth(44) + breathingRoom(30)`を加算し、アイコン全体+余白が
   見える位置まで確実にスクロールするよう修正。
2. **効果帯の名前が見えなくなる問題**: 前回の修正で意図通り。追加対応なし。
3. **待機の削除×、ホバーはできるようになったが依然反応しない**: 真因は2つあった。
   (a) 各行の`.timeline-track`は空の透明な箱だが、`position:relative`により
   「positioned, z-index:auto」レイヤーへ参加するため、DOM順で後にあるアビリティ行のtrackが、
   GCD行内に描画された待機(z-index:0)より前面でヒットテストを奪っていた
   ―― 待機のうちアクションと重ならないはずの下部(アビリティ行の高さにかかる部分)ですら、
   隣のアビリティ行track自身に当たり判定を食われ、ホバーが一切発火しないケースがあった。
   `.timeline-track`を`pointer-events: none`にし、実際の操作対象(`.timeline-action`、
   `.timeline-action-delete`、`.timeline-effect`)側だけ`pointer-events: auto`で復元して解決。
   (b) 削除×がホバー確実領域(下部)から離れた上部(GCD行側)に配置されていたため、ホバー発火→
   削除×クリックへマウスを動かす間に隣接アクション上を一瞬通過し、その時点でグループホバーが
   外れて削除×が消えてしまうケースがあった。削除×自体もホバー確実領域(下部)へ再配置し、
   ホバー開始地点とクリック先の距離をなくして解決。
4. **待機アイコンの上辺をtimeline-axisに接するようにしたい**: `.timeline-action.is-wait`の
   `top`を`8px`→`0`に、`height`を`117px`→`125px`に変更(下端の絶対位置は変えずに上端だけ
   axis直下まで伸ばす)。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン。
- ブラウザでの実動作確認済み(Chrome拡張経由)。本セッション中、CDP経由の`left_click`が
  ページに一切イベントを届けない(document捕捉フェーズでも検知不能)という形で複数回失敗する
  不安定さが見られたため、`elementFromPoint`によるヒットテスト確認に加え、実際の
  `pointerdown`/`mousedown`/`pointerup`/`mouseup`/`click`一式を対象要素へ`dispatchEvent`する
  方式で最終確認し、正しく削除されることを確認した。

### 待機の削除×、依然クリック不成立の修正 — 完了(2026-08-28)

第2弾の修正後もユーザーから「ホバーはできるようになったが×がまだ押せない」との報告。位置(右下)は
問題なしとのこと。

**根本原因**: 削除×をホバー中だけDOMへ出し入れ(条件付きレンダリング)していたため、実際の
クリック動作(mousedown→mouseup)の間にマウスがほんの僅かでもぶれてグループホバーが外れると、
その瞬間に削除×ごとアンマウントされ、mouseupの着地先が消えてclickイベントが不成立になる
ケースがあった。自動化したCDP経由のクリック(座標固定・手ぶれなし)では再現しにくく、実際の
マウス操作でのみ顕在化しやすい種類の不具合だった。

**対応**:
- 削除×は常にDOMへマウントしたままにし、表示/非表示は`opacity`+`pointer-events`の切り替えのみで
  行うよう変更(要素そのものがクリック中に消えないようにする)。
- 加えて、`mouseleave`から実際に非表示にするまで200msの猶予を設け、猶予中に再度ホバーへ戻れば
  非表示化をキャンセルする(`WaitTile`に`hideTimerRef`を追加)。クリック中の一瞬のホバー離脱を
  吸収するための保険。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン。
- ブラウザでの実動作確認済み(Chrome拡張経由)。今回、CDP経由のクリック座標指定を誤って
  (スクリーンショットの実際の解像度を取り違えて)いたことが判明し、正しい座標で再検証したところ
  クリックは正常に機能した。あわせて、ホバーが一瞬外れてすぐ戻る手ぶれのシナリオを
  `dispatchEvent`で再現し、削除が正しく成立することも確認した。

### 待機の削除×、真の根本原因を特定・修正 — 完了(2026-08-28)

前回の修正(常時マウント化)後も「ホバーはできる・カーソルは指先になる・クリックしても一切
反応がない」との報告。実際にはデプロイ済みのGitHub Pagesが最新コミットであることを確認した上で
再調査し、真の原因を特定した。

**真因**: `.time-indicator`(表示位置を示す青い縦線、スクラブ操作のため`cursor: ew-resize`で
常時インタラクティブ)が`z-index: 2`で、待機の削除×(`.timeline-wait-delete`)も同じ`z-index: 2`
だった。待機を追加した直後、`表示位置`(displayTime)はちょうど**その待機の終了時刻**になる
(=削除×を配置している待機の右端と同じ座標)。つまり削除×とタイムインジケーターは、待機を
追加した直後ほぼ確実に同じ座標へ重なる。z-indexが同値の場合はDOM順が後の要素が勝つが、
`.time-indicator`は`.timeline-row-gcd`/`.timeline-row-ability`より後にレンダリングされるため、
常にタイムインジケーターが削除×の上を覆い、クリックをすべて奪っていた（カーソルが指のまま
なのは、削除×のごく一部が視覚的に露出していてそこにカーソルが乗っていたため。しかし実際の
クリック座標がタイムインジケーターの帯と重なっていれば、そちらにイベントが渡っていた)。

実測でも確認: 待機追加直後の`.time-indicator`矩形と`.timeline-wait-delete`矩形は、
横方向に約6px、縦方向は完全に重なっていた。

**解決策**: `.timeline-wait-delete`の`z-index`を`2`→`7`に変更(`.time-indicator`の`2`は
もちろん、通常アクションの削除×`z-index:6`よりも上)。加えて、前回対応した「クリック中の
ホバー離脱で要素が消える」問題への保険として、当たり判定(pointer-events)はホバー状態に
一切依存させず常時有効にし、`opacity`のみで見た目を切り替える形に単純化した(見た目と
クリック可否のタイミングが分離するリスクを排除)。

- `tsc --noEmit`・`npm run build`・Vitest 37件、すべてクリーン。
- ブラウザでの実動作確認済み(Chrome拡張経由)。今回は実際にCDP経由の`hover`→`left_click`
  (座標のスクリーンショット/実解像度換算を都度確認した上で)で削除が成立することを確認した。

### 未着手のマイルストーン

- **M11**(旧M9): CSS最終調整、本セクションを含むPLAN.md全体の新アーキテクチャ反映への書き直し、実戦的な
  リーパー回しでlegacy版と新版の最初から最後までの一致確認。

### 開発サーバー起動方法(再開時)

```
cd C:\Users\toshiki\Documents\cloude_code\ff14-simukator
npm run dev
```
`legacy/index.html`は`legacy/`フォルダをローカルサーバーで配信すれば単体で動作する(旧仕様との比較用に
残してある。削除しないこと)。

検証用スクリプト: `scripts/cross-check-legacy.cjs`(Node vm上でlegacy/js/engine.jsを実行し、指定した
スキルID配列を`useSkill`で順に実行して最終状態をJSON出力する。新エンジンとの数値突き合わせに使う。
使い方: `node scripts/cross-check-legacy.cjs '["soulSlice","stalkSwathe",...]'`)。
