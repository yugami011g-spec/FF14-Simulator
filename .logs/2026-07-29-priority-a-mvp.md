# 2026-07-29 優先度A MVP整備

## 変更
- `PLAN.md`、画面文言、JavaScriptコメントの文字化けを修正した。
- スキル表示、合計威力、コンボ段階、履歴ログのラベルを自然な日本語にした。
- リセットボタンを追加し、`resetSimulator()` で `state` を初期化するようにした。
- 履歴ログに空状態を追加し、手数、スキル名、威力、合計、コンボ段階を行ごとに表示した。
- スキルデータに `type`、`comboPotency`、`requiredComboStep`、`recast`、`gcd`、`effects` を揃えた。

## 確認
- `node --check` 相当で `js/skills.js`、`js/state.js`、`js/engine.js`、`js/ui.js` の構文チェックを通した。
- 代表的な文字化け断片を `rg` で検索し、ヒットなしを確認した。
- ローカルHTTPサーバーでブラウザ確認した。
- コンボ1→2→3で合計威力920、コンボ段階3になることを確認した。
- リセット後にコンボ2を押すと通常威力150、コンボ段階0になることを確認した。
- デスクトップ幅とモバイル幅で横はみ出しなし、コンソールエラーなしを確認した。
